name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build with maven
        run: mvn clean install
      
          
     
              
